node_modules/

# Recorded output. These are full DOM clones of whatever was on screen —
# real UI, real data, real internal URLs. Never commit them; ship the tool,
# not the recordings.
out/
video/
*.webm
*-steps.json
*-trace.zip
*.tgz

# Live attach-mode session state
.session.json
.stop

.DS_Store
.playwright-cli/

# Flows against private/internal apps: kept locally, never published.
flows/local/
